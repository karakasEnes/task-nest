{
  "name": "nestjs-fullstack",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Enes Karakas <enes.coding@gmail.com>",
  "license": "MIT",
  "scripts": {
    "client:start": "yarn --cwd client/ start",
    "server:start-dev": "yarn --cwd server/ start:dev",
    "build:win": "yarn --cwd client/ build-move:win",
    "build:unix": "yarn --cwd client/ build-move:unix"
  }
}
